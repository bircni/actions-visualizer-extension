name: Gitea CI
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: make build
  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: make release
