name: Reusable
on:
  schedule:
    - cron: "0 3 * * 1"
jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - run: echo prepare
  call:
    needs: prepare
    uses: ./.github/workflows/simple.yml
    secrets: inherit
